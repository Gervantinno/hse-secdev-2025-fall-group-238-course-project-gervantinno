# Trivy ignore file
# Уязвимости, которые игнорируются с пояснением для P07

# Примечание: для продакшна нужно регулярно обновлять базовый образ
# и зависимости, чтобы устранять уязвимости.

# Пример игнорирования (раскомментируй и добавь CVE ID, если нужно):
# CVE-2023-XXXXX  # Ложное срабатывание в python:3.12.7-slim, исправлено в upstream
# CVE-2024-XXXXX  # Уязвимость в dev-зависимости, не влияет на runtime

# Для просмотра всех найденных уязвимостей см. артефакт trivy-report.txt в CI
